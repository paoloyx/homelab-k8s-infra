# Default values for stack.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# target branch taken as global revision
global:
  targetRevision: main

# Stack related values
stack:
  crossplane:
    version: 1.9.0